# Dotfiles

This repo manages my shell and editor configuration on WSL:

- zsh (Oh My Zsh)
- Neovim (LazyVim)
- git
- other CLI tools

Managed as a bare repo in `$HOME` using:

- git-dir: `~/.dotfiles`
- work-tree: `~`
